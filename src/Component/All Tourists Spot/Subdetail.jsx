import React from 'react'

export default function Subdetail({ data }) {
    const { _id, photo, name, countryname, location, time, yearcost, seasonality, cost, description } = data
    return (
        <div>
            
        </div>
    )
}
